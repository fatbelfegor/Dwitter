<!-- we will render separate nav bars depending on if the usersigned in or not -->
<% if !user_signed_in?  %>
  <%= render '/layouts/nav_guest' %>
<% else %>
  <%= render '/layouts/nav_user' %>
<% end %>